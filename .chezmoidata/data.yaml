packages: 
  base: git zsh curl wget fontconfig xdotool pipx
  buildenv: build-essential libssl-dev zlib1g-dev libbz2-dev libreadline-dev libsqlite3-dev curl libncursesw5-dev xz-utils tk-dev libxml2-dev libxmlsec1-dev libffi-dev liblzma-dev

distros:
  arch:
    - arch
  ubuntu:
    - ubuntu debian
    - ubuntu

nerdfonts:
  fonts:
    - CascadiaCode
    - CascadiaMono
    - FiraCode
    - FiraMono

ohmyzsh:
  plugins:
    - zsh-syntax-highlighting
    - zsh-autosuggestions

